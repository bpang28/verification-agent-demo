# Pinned requirements for the code supplement.
# Tested with Python 3.11. Install with: pip install -r requirements.txt

anthropic>=0.111,<1
pydantic>=2.5,<3
python-dotenv>=1.0,<2

# Vector retrieval — fastembed uses ONNX (no torch/CUDA, ~25 MB model download).
# Embeddings stored as a compressed numpy file; cosine search is pure numpy.
# In production this is replaced by a LanceDB Cloud Run service.
fastembed>=0.3,<2
numpy>=1.24,<3
