# Pinned requirements for the code supplement.
# Tested with Python 3.11. Install with: pip install -r requirements.txt

anthropic>=0.111,<1
pydantic>=2.5,<3
python-dotenv>=1.0,<2

# Vector retrieval (run infrastructure/build_index.py once after install)
lancedb>=0.8,<2
sentence-transformers>=2.6,<5
numpy>=1.24,<3
